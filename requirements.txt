# Claude API for structured resume extraction
anthropic>=0.40.0

# PDF text extraction — better than PyPDF2 for multi-column resume layouts
pdfplumber>=0.11.0

# JSON Schema validation of extracted resume data
jsonschema>=4.23.0
